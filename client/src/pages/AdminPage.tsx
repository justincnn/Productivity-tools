import { Box, Typography } from '@mui/material';

export default function AdminPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Admin Panel</Typography>
      <Typography>Admin tools will go here.</Typography>
    </Box>
  );
}
