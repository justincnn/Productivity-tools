import { Box, Typography } from '@mui/material';

export default function MainPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Main Application</Typography>
      <Typography>Content will go here.</Typography>
    </Box>
  );
}
